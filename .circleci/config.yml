version: 2.1

orbs:
  riskiops: riskified/riskiops@0 # use this orb to deploy your application
  image-builder: riskified/image-builder@0 # use this orb to build docker images

jobs:
  build:
    docker:
      - image: 284309667046.dkr.ecr.us-east-1.amazonaws.com/riskified/images-library/node:master-18.16.1-alpine3.18-ci
    steps:
      - checkout
      - run:
          name: Say Hello
          command: echo "Hello, CircleCI!"

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      
